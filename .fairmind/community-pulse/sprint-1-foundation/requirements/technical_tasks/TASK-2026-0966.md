# TASK-2026-0966: Setup pnpm Monorepo Workspace with Three Workspaces and Docker Compose

**FairMind ID**: 6a15c19d6c2398cc3a8cda76
**User Story**: US-2026-0589
**Session**: SESSION-2026-0192

## Acceptance Criteria (Authoritative — Validation Contract)

- [ ] `pnpm-workspace.yaml` exists at repo root and declares `apps/*` and `packages/*`
- [ ] Root `tsconfig.base.json` defines the `@community-pulse/shared` path alias pointing to `packages/shared/src`
- [ ] All three workspaces (`packages/shared`, `apps/api`, `apps/web`) have `package.json` and `tsconfig.json` that extend the base config
- [ ] `pnpm install` completes without hoisting errors or version conflicts
- [ ] `pnpm -r run build` (or `pnpm build`) compiles all three workspaces with zero TypeScript errors
- [ ] A test import of `@community-pulse/shared` in both `apps/api/src/index.ts` and `apps/web/src/main.tsx` resolves correctly during `pnpm typecheck`
- [ ] `docker-compose.yml` includes `mongodb` (mongo:7), `app`, and optionally `mongo-express` services
- [ ] `docker compose up` starts all services without manual intervention; MongoDB data persists via named volume
- [ ] `.env.example` documents `MONGO_URI`, `JWT_SECRET`, `PORT`, `APP_ORIGIN`, and `NODE_ENV`
- [ ] `README.md` contains working local setup instructions

## Files to Create/Modify

- `pnpm-workspace.yaml`
- `package.json` (root)
- `tsconfig.base.json` (root)
- `docker-compose.yml` (root)
- `.env.example` (root)
- `.gitignore`
- `.nvmrc`
- `packages/shared/package.json`
- `packages/shared/tsconfig.json`
- `packages/shared/src/index.ts`
- `apps/api/package.json`
- `apps/api/tsconfig.json`
- `apps/api/src/index.ts`
- `apps/api/Dockerfile`
- `apps/web/package.json`
- `apps/web/tsconfig.json`
- `apps/web/src/main.tsx`
- `README.md` (update)
